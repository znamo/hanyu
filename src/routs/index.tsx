import RecognizeCharacters from "../components/recognize_characters/RecognizeCharacters";

const routes = [
    {
        path: "/recognize",
        component: RecognizeCharacters
    }
];

export default routes;